6/7/2026
- Adequate setup for the initial basic SignalR connection
- Adequate minimal setup command processor, LookCommand dispatch, including DI setup
- Architecture failure when telling it to send messages back to the client.  SignalR infrastructure details leaked into the rest of the app, including command processing and command handlers.
	- Had to give architectural guidance to abstract away SignalR infrastructure from the command handling code.
		- It produced an adequately abstracted version of the client (for now)
- When I told it to send room data to the client, it assumed I meant concatenating the string inside the LookHandler.  I wanted to send the actual data
	- Adequately abstracted the room data to text conversion after guidance
- Adequately converted the output on the index.html page to a list of divs.
- Major archetectural failure when I asked it to generate a set of rooms reflected a chessboard.  Added a bunch of logic, including generating the data to the StaticWorldDataService
	- It failed to separate the concerns of serving world data vs. generating a chessboard world.
	- Adequately refactored into ChessworldBuilder after guidance.
	- It failed to identify "direction" as an enum candidate.
- Adequately handled adding the player current room ID lookup to LookHandler.
- It added the MoveHandler but made a few blunders.
	- Bizarrely allowed movement to a non-existent room including a message to the player saying they "moved to nowhere."
	- It also did not attempt to abstract the idea of commands having names and parameters and embedded the parsing directly in the MoveCommandHandler.
	- It fixed the "moved to nowhere" problem after guidance.
- Adequately added command history, focus, and clearing to the input box.  Did not consider scope of variables such as commandHistoryIndex.
- Duplicated mob look up and room data construction when I asked it to add Mobs and send list of mobs with room data.
	- Its solution after guidance was somewhat unexpected.  It added a RoomPresenter instead of a common data builder.  I'm going to let it ride for now.
- Failed to separate the concerns of building chessboard mobs from the general mob data service.  This is the same failure that happened with room data.
	- Adequately separted the data after guidance, but failed to make the chessboard builder cohesive and left it in two separate folders.
- Excelled at setting up two panes for two connections on the test page
- Adequately added players to the room description and randomized test player names.
- Architectural failure when I asked to serialize all player commands.  It added the serialization logic to the Player class instead of the command processor and serialized them per-player instead of across the app.
	- It created a simple semaphore-based queue after guidance, we'll see how that works out.
- Adequately filtered out the current player self-description from room presentation.
- Failed to separate the idea of players in a room from player connections and implemented lookup of players in a room via signalR.
	- Separated the player data service after guidance but used a bizarre IDictionary<Player,byte> with a byte value of 0 for all players.  Presumably because there is no player ID currently.
	- Adequately resolved this issue after guidance.
- Excelled at creating shortcut handlers for north, south, east, west.  Adequately separated move logic from the handlers and reused it across handlers.
- Added the KillCommand but it still did not abstract the parameter parsing.
	- Adequately separated command parsing after guidance (although it's a bit more complex than required, probably).
- Adequately created a running fight with 2 second delay for a single player / single mob fight.
- Completely failed when I told it that a player cannot issue any commands while in a fight state.  It put the fight state check in the SignalR Hub instead of at the application level.
	- Adequately separated the fight check after guidance.  It did not predict that we would have more complex state-based rules so we will see how it reacts to the next prompt.
- Failed again when I asked it to add a flee command which can be issued during a fight.  It added this logic directly to the CommandExecutor with hardcoded "flee" string.
	- Adequately resolved this after guidance.  Did not anticipate many possible states, so it's still very specific to "IsInFight"
- It created a bug where the flee command cannot be processed during a fight and are all processed after the fight completes.
	- It is failing to recognize that there should be separation between command handling and the fight processing.  I need to give it very specific guidance.
	- Adequately separated the fight processing from the command queue.  However, it's not thread safe and there are race conditions between flee and the fight loop.
	- I gave it specific guidance about the flee/fight race condition and it created a semaphore on Player and sent the logic of both through a higher-order function there.
		- I gave it guidance on creating a game loop, which put it into plan mode
		- Adequately refactored into a game loop with command queue and hosted service after being given very specific guidance
			- The CommandQueue had a bug where it would endlessly drain under heavy input and prevent the rest of the game logic from updating.  I asked it to plan a solution and it did not provide the solution I was looking for.
			- Adequately converted it to draining all commands per iteration snapshot.
		
	

