6/7/2026
- Adequate setup for the initial basic SignalR connection
- Adequate minimal setup command processor, LookCommand dispatch, including DI setup
- Architecture failure when telling it to send messages back to the client.  SignalR infrastructure details leaked into the rest of the app, including command processing and command handlers.
	- Had to give architectural guidance to abstract away SignalR infrastructure from the command handling code.
		- It produced an adequately abstracted version of the client (for now)
- When I told it to send room data to the client, it assumed I meant concatenating the string inside the LookHandler.  I wanted to send the actual data
	- Adequately abstracted the room data to text conversion after guidance
- Adequately converted the output on the index.html page to a list of divs.
- Major archetectural failure when I asked it to generate a set of rooms reflected a chessboard.  Added a bunch of logic, including generating the data to the StaticWorldDataService
	- It failed to separate the concerns of serving world data vs. generating a chessboard world.
	- Adequately refactored into ChessworldBuilder after guidance.
	- It failed to identify "direction" as an enum candidate.
- Adequately handled adding the player current room ID lookup to LookHandler.
- It added the MoveHandler but made a few blunders.
	- Bizarrely allowed movement to a non-existent room including a message to the player saying they "moved to nowhere."
	- It also did not attempt to abstract the idea of commands having names and parameters and embedded the parsing directly in the MoveCommandHandler.
	- It fixed the "moved to nowhere" problem after guidance.
- Adequately added command history, focus, and clearing to the input box.  Did not consider scope of variables such as commandHistoryIndex.
- Duplicated mob look up and room data construction when I asked it to add Mobs and send list of mobs with room data.
	- Its solution after guidance was somewhat unexpected.  It added a RoomPresenter instead of a common data builder.  I'm going to let it ride for now.
- Failed to separate the concerns of building chessboard mobs from the general mob data service.  This is the same failure that happened with room data.
	- Adequately separted the data after guidance, but failed to make the chessboard builder cohesive and left it in two separate folders.
- Excelled at setting up two panes for two connections on the test page
- Adequately added players to the room description and randomized test player names.
- Architectural failure when I asked to serialize all player commands.  It added the serialization logic to the Player class instead of the command processor and serialized them per-player instead of across the app.
	- It created a simple semaphore-based queue after guidance, we'll see how that works out.
- Adequately filtered out the current player self-description from room presentation.
- Failed to separate the idea of players in a room from player connections and implemented lookup of players in a room via signalR.
	- Separated the player data service after guidance but used a bizarre IDictionary<Player,byte> with a byte value of 0 for all players.  Presumably because there is no player ID currently.
	- Adequately resolved this issue after guidance.
- Excelled at creating shortcut handlers for north, south, east, west.  Adequately separated move logic from the handlers and reused it across handlers.
- Added the KillCommand but it still did not abstract the parameter parsing.
	- Adequately separated command parsing after guidance (although it's a bit more complex than required, probably).
- Adequately created a running fight with 2 second delay for a single player / single mob fight.
- Completely failed when I told it that a player cannot issue any commands while in a fight state.  It put the fight state check in the SignalR Hub instead of at the application level.
	- Adequately separated the fight check after guidance.  It did not predict that we would have more complex state-based rules so we will see how it reacts to the next prompt.
- Failed again when I asked it to add a flee command which can be issued during a fight.  It added this logic directly to the CommandExecutor with hardcoded "flee" string.
	- Adequately resolved this after guidance.  Did not anticipate many possible states, so it's still very specific to "IsInFight"
- It created a bug where the flee command cannot be processed during a fight and are all processed after the fight completes.
	- It is failing to recognize that there should be separation between command handling and the fight processing.  I need to give it very specific guidance.
	- Adequately separated the fight processing from the command queue.  However, it's not thread safe and there are race conditions between flee and the fight loop.
	- I gave it specific guidance about the flee/fight race condition and it created a semaphore on Player and sent the logic of both through a higher-order function there.
		- I gave it guidance on creating a game loop, which put it into plan mode
		- Adequately refactored into a game loop with command queue and hosted service after being given very specific guidance
			- The CommandQueue had a bug where it would endlessly drain under heavy input and prevent the rest of the game logic from updating.  I asked it to plan a solution and it did not provide the solution I was looking for.
			- Adequately converted it to draining all commands per iteration snapshot.
- Adequately added command name shortcuts
- Adequately separated Program.cs DI registrations into logical units
- Adequately created BareHandDamage and updated FightService
- Failed to use abstracted Character object to handle damage calculation during fight update which created duplication in code.
	- Adequately refactored to remove duplication after specific guidance was given.
- Adequately added HTML formatting and color coding, but added all formatting directly in SignalRClient class.  This could lead to a huge class with formatting for all messages.
- Adequately added a default "look" command on SignalR connect but failed (again) to process that SignalR is only one client type.
	- When given guidance to separate concerns, created "PlayerEntryService" instead of creating a higher level abstraction at the game level.
	- Adequately fixed this issue after being given specific guidance.
- Excelled in refactoring the IsInFight flag to be a state enum called CharacterState with multiple states.
- When I asked it to block commands based on Dead state, it did not identify any abstraction and added a new policy specific to dead state.  I'm leaving it for now.
- Adequately added a grouping mechanism but failed to recognize that it allows players to issue move commands while in a state that should not be allowed.
	- Corrected issue with guidance
- Adequately added group fight dynamics
- Added conditional room formatting.

Overall impression for 6/7/2026:
I was extremely productive generating code for a system that I already understand pretty well.  I don't know if I would have the same success with a problem domain which I haven't previously explored.
Cursor made the code generation faster and easier.  Cursor also made several mistakes including some severe architectural mistakes.  I continue to believe that very close supervision is required to 
create high-quality code with Cursor.

6/8/2026
- Adequately created a build pipeline for GitHub actions + Azure app service deployment.
- Adequately created a help command.  Required guidance to exclude help from the command list.
- Adequately added an "unknown command" message.		
	
6/13/2026 (using Opus 4.8 model)
- Adequately created PlayerData and PlayerDatabase code.  Strangely, it didn't rename the database class correctly to match the interface name I requested.
- Adequately added account storage and verification with password hashing	
- Created an abomination of API endpoints in a class called AuthEndpoints, putting all log for all endpoints inside a master Map statement.  
	- I personally don't like minimal APIs so I had it move them to an MVC controller.
- I didn't realize it had created a Register endpoint and asked it to create one.  It correctly detected that it already existed.
- Adequately created a login.html page with flows back and forth from index.html as requested
- Excelled at splitting index.html into mud.html (for game UI) and a lightweight router page.
- Excelled at creating a stateful workflow for player creation.
	- Failed again to abstract the concept of player connection from the signalR interactions.
		- Adequately refactored to a transport-agnostic connection abstraction after guidance.
- Failed at adding BaseDataDirectory config option.  Added an invalid fallback based on ContentRoot and did not fail fast on startup.
	- Adequately refactored and created configuration class based on guidance.
- Correctly identified a mistake in my directions to rename the html files and gave me an opportunity to fix it.
- After guidance, it consolidated styles and extracted them to a common css file
- Excelled at generating a dark theme based on the Cursor default theme.
- Adequately added a "stats" update message to the signalr output and integrated it with the fight service.  This will need to be more global eventually but this is fine.
- Excelled at integrated this new message with the SignalR flow and the html.
- Excelled at identifying the correct place to put the initial PlayerStatsUpdate (AddPlayerToGame)
- Adequately added a Welcome message and gave me a "content" file for the message after guidance
	- It abstracted sending an "Html" message which was better than I expected.
- Completely failed to vertically center the footer/status bar "connected" area.

Overall impression for 6/13/2026
I generated a lot of very useful functionality today and made a log of progress on the project.  I didn't find Opus 4.8 significantly better than my exprerience with the "Auto" mode
but it was mildly better and required less explicit guidance.  I was able to get a lot of CSS implemented to my satisfaction but I'm not spending any time reviewing that so I
haven't evaluated the quality of what it has done there.  CSS with dynamic layouts is usually a source of grief for me so I appreciated being able to get something very functional
with only high level prompts.

Next time: 
	- Add class selection to character creation.
	- Add level + xp to player data
	- Add spell casting (for mage/cleric) and mana stats

6/14/2026 (Auto mode)
- Adequately added class, levels, and xp fields to Players
	- It identified the missing class property in existing data but it did not offer the option of directly migrating the current data files.  It did this after guidance.
	- Incorrectly assumed that this data should be added to PlayerStatsUpdate and removed this code after guidance.
- Excelled at adding "spells" from a very high level prompt "mages should have spells, how should we add this?"
	- Plugged into the current architecture correctly.
- Failed to identify the pattern of current/max value objects.
	- Excelled at adding a "ResourcePool" value object after guidance.
- Excelled at creating a new world area "Hockesden" based on directions about what the world should contain and where rooms are connected.
	- Excelled at integrating Hockesden world with the current world data patterns.
- Adequately added Exits command and AutoExits toggle command.
- Adequately added Portal room and recall command
- Failed appending natural exit descriptions to room descriptions.
	- Adequately generated prose descriptions after additional guidance.
- Excelled at adding aggressive mobs with very specific implementation guidance.  Integrated well with existing fight code.
- Adequately extracted the Hockesden area to a json file
- Absolutely comically failed at extracting the chessboard to a file.  
	- Instead of explicitly storing the data, it stored data that allowed it to dynamically build the world but required code specific to chessboard area loading.
	- When I asked it to create an "areas.json" list of worlds, it tacked on a "Loader" property to specify dynamic loaders for each type.
	- Adequately refactored to a common data file format after guidance
- Adequately separated mob definitions from mob spawns to enable multiple spawns per mob definition
- Failed when adding more spells which are not specifically "damage" or "heal" in effect.  Spells need to be fully generic and allowed to have any effects
	- It added a weird SpellEffect enum with heal, damage, and recall to deal with the three types of spells instead of making spell effects fully extensible.
	- Adequately refactored into spell classes after guidance.
- Partially successful on abstracting file storage details into local file and azure blob storage.
	- Adequately implemented interfaces and DI configuration
	- Failed to separate the configuration for local file store from azure blob store.  Included "connection string" in a generic "file storage" options class.
- Failed to create a default constructor when Room was moved to a json file.
	- Adequately corrected the issue after being given the exception message.

6/19/2026
- Adequately added a leveling system but had to be refactored to allow "You leveled up!" message to be sent to the player.
- Adequately added logic for calculating experience awards on mob death during fights.